<turbo-frame id="dash_grid">
  <%= yield %>
</turbo-frame>
<%# Here, the yield will be replaced when the "dash_grid" is is being called.
The partials _clientsGrid etc... THIS IS JUST A WRAPPER since it didn't work without it. No idea why
-> home.html.erb %>
